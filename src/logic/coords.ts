export type Point = {
  x: number;
  y: number;
};

export type Polar = {
  r: number;
  t: number;
};

export type QuadrantCoords = {
  radial_min: number;
  radial_max: number;
  factor_x: number;
  factor_y: number;
};

export type RingCoords = {
  radius: number;
};
